{form form}

    {foreach $peoples as $people}
        <div class="form-check">
            <input
                    type="checkbox"
                    name="femaleRelation[]"
                    value="{$people->id}"
                    id="femaleRelation{$people->id}"
                    {if array_key_exists($people->id, $selectedPeoples)} checked="checked"{/if}
            >
            <label class="form-check-label" for="femaleRelation{$people->id}">{$people->name} {$people->surname}</label>
        </div>
    {/foreach}

    {input send}

{/form}