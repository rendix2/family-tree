{block title}
    {_menu_country}
{/block}

{block breadcrumb}
    <li class="breadcrumb-item active" aria-current="page">{_menu_country}</li>
{/block}

{define modal-countryDeleteFromList-title}
    {_country_delete_country, [$countryModalItem]}?
{/define}

{define modal-countryDeleteFromList-body}
    {control countryDeleteFromListForm}
{/define}

{block content}
    <h1>{include title}</h1>

    <div class="float-right mb-1">
        <a href="{link Country:edit}" class="btn btn-primary">
            <i class="fas fa-plus"></i> {_country_add_country}
        </a>
    </div>

    {snippet list}
    <table class="table table-bordered table-hover table-striped">
        <thead>
        <tr>
            <th>{_country_id}</th>
            <th>{_country_country}</th>
            <th width="20%"></th>
        </tr>
        </thead>
        <tbody>
        {foreach $countries as $country}
            <tr>
                <td class="align-middle">{$country->id}</td>
                <td class="align-middle">{$country|country}</td>
                <td>
                    <a href="{link Country:Edit, $country->id}" class="btn btn-primary btn-block">
                        <i class="fas fa-edit"></i> {_country_edit_country}
                    </a>
                    <a href="{link countryDeleteFromList!, $country->id}" class="ajax btn btn-danger btn-block">
                        <i class="fas fa-trash"></i> {_country_delete_country, ['']}
                    </a>
                </td>
            </tr>
        {/foreach}
        </tbody>
    </table>
    {/snippet}
{/block}
